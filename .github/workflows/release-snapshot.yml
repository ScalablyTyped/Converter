# note that the proper release is made manually with the release script because it's
# a heavy operation (builds all demo repos)
# that's why we don't trigger on tag
name: Release snapshot
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  release-snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: coursier/cache-action@v6.4
      - uses: coursier/setup-action@v1
        with:
          apps: sbt

      - name: Release
        run: sbt ci-release
        env:
          PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
          SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
          SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
          # Custom release command: publish everything for 2.12 (which yields the sbt 1.x plugin),
          # then everything for Scala 3 (which yields the sbt 2.x plugin)
          CI_RELEASE: >-
            ++2.12.20;
            logging/publishSigned;
            core/publishSigned;
            phases/publishSigned;
            ts/publishSigned;
            scalajs/publishSigned;
            importer-portable/publishSigned;
            sbt-converter/publishSigned;
            importer/publishSigned;
            cli/publishSigned;
            ++3.9.0;
            logging/publishSigned;
            core/publishSigned;
            phases/publishSigned;
            ts/publishSigned;
            scalajs/publishSigned;
            importer-portable/publishSigned;
            sbt-converter/publishSigned;
            importer/publishSigned;
            cli/publishSigned
